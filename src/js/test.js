var storyPromise = false;
var obj = true
storyPromise = storyPromise || obj;
console.log(storyPromise)

// setTimeout(test, 5000, 'three', '2')

// function test(v, v2) {
//     console.log(v, v2)
// }
